import React from 'react';
import './Header.css'; // Import the CSS file for additional styles

const Header = () => {
  return (
    <header className="glowing-header">
      Lost and Found Pets Platform
    </header>
  );
};

export default Header;
